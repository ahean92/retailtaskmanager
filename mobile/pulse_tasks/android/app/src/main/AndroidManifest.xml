<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network access for the lsFusion HTTP API -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Location: the app is not entered without it (see lib/ui/geo_gate_screen.dart).
         Both, and not FINE alone: from Android 12 the system dialog offers «точное» and
         «приблизительное», and a request for FINE that does not also ask for COARSE is
         ignored outright. A block's worth of accuracy is all this gate asks for anyway. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Пуш (#36720). Начиная с Android 13 показ уведомлений — такое же разрешение, как
         камера: без запроса система молча не показывает ничего, и приложение об этом не
         узнаёт. На версиях до 13 разрешение считается выданным. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="pulse_tasks"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Нажатие на уведомление должно поднимать именно это Activity, чтобы
                 firebase_messaging отдал сообщение в onMessageOpenedApp и приложение
                 открыло нужную задачу. Парная половина — "click_action" в теле,
                 которое шлёт сервер (PushFcm.lsf). -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Канал уведомлений по умолчанию. Идентификатор совпадает с channel_id, который
             сервер кладёт в android.notification (PushFcm.lsf): не совпади они — Android
             положит уведомление в безымянный канал «Разное» с низким приоритетом, то есть
             без звука и без всплытия, что для срочной задачи равносильно недоставке. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="pulse_tasks"/>
        <!-- Силуэт, а не цветная иконка приложения: с Android 5 система перекрашивает
             значок в шторке в белый, и всё, что не силуэт, превращается в белый квадрат. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
