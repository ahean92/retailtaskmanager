# Следы запуска и разработки: в артефакт не входят, между машинами не переносятся.
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
# Настройки конкретной машины — модель, порты, уровень журнала. Образец лежит рядом
# в .env.example, а сам .env у каждого свой.
.env
